---
name: trusted-procurement-agent
description: 매일 아침 식자재 시세를 점검해 먼저 볼 품목을 골라 브리핑 한 장으로 남긴다
model: solar-open2
schedule: "0 6 * * *"
trigger_prompt: |
  🔴 이 실행에서는 사람에게 아무것도 묻지 않는다. 예약 실행이므로 아무도 없다.

  1. export TPA_DATA_DIR=./tpa
     (.pi/ 아래에 쓰면 다음 턴에 사라져 「어제와 같은가」를 판정하지 못한다)
  2. python3 .pi/skills/trusted-procurement-agent/scripts/agent.py 를 실행한다.
     DATAGO_KEY 가 환경에 있으면 실시간 수집을, 없으면 동봉 스냅샷을 쓴다.
     🔴 키가 없어도 request_env_var 를 호출하지 않는다. 그냥 진행한다.
  3. 출력 첫 줄의 상태로 갈라진다.
     · 「변화 없음」        → 여기서 끝낸다. 화면을 다시 만들지 않는다.
     · 「새 자료 처리」      → 4번으로
     · 「이월분 재개」       → 4번으로 (실패했다 회복한 것도 새 산출물이다)
     · 「실패」             → 화면을 만들지 않고 5번으로
  4. create_artifact(from_path="./tpa/output/report.html",
                     title="오늘 먼저 볼 것") 로 화면을 띄운다.
  5. 응답에 셋만 적는다 — 상태 · 「↳」로 표시된 어제와의 차이 · 「⚠」로 표시된 문제.
     산출물 내용을 다시 요약하지 않는다. 화면에 이미 있다.
     숫자를 새로 만들거나 바꾸지 않는다.
---

# Trusted Procurement Agent

매일 아침 6시(Asia/Seoul), **사람이 없어도 혼자 돈다.**

## 하는 일

```
오늘 시세 조회 (호출 1회)
   └ 어제와 같으면 → 여기서 끝
   └ 다르면 ↓
수집 → 판정 → 어제와 비교 → 화면
```

## 🔴 예약 실행에서 지켜야 할 것

**① 아무것도 묻지 않는다**

인증키가 없으면 **동봉 스냅샷으로 진행한다.** `request_env_var` 를 부르면
예약 실행이 사람 응답을 기다리며 멈춘다 — 그 순간 Agent 가 아니게 된다.

키 등록은 **사람이 대화로 부를 때만** 한다([SKILL.md](SKILL.md) 「준비」 절).

**② 데이터는 워크스페이스 루트 아래에 쓴다**

`.pi/skills/` 는 매 턴 라이브러리에서 다시 만들어진다. `TPA_DATA_DIR=./tpa` 를
설정해야 `./tpa/state/last_run.json` 이 살아남아 **「어제와 같은가」를 판정**할 수 있다.

**③ 변화가 없으면 아무것도 만들지 않는다**

같은 화면을 매일 다시 그리면 사람이 읽지 않게 된다.

**④ 「이월분 재개」도 새 산출물이다**

실패했다가 회복한 주기는 **새로운 판정을 낸 것**이다. 화면을 띄운다.
이 분기를 빠뜨리면 사용자는 회복 사실을 모른다.

**⑤ 숫자를 만들지 않는다**

`report.json` 에 있는 값만 옮긴다. 응답에 쓸 문장이 필요하면
`agent.py` 출력의 「↳」 줄을 그대로 쓴다.

## 사람이 부를 때 (대화형)

- 인증키를 처음 넣을 때 — `request_env_var` 로 `DATAGO_KEY` 요청
- 취급 품목을 고를 때 — `create_spreadsheet` 로 `reference/item_map.csv` 를 띄움

**둘 다 선택이지 실행 조건이 아니다.** 하지 않아도 예약 실행은 계속 돈다.
