# 산출물 — 매 실행마다 바뀐다. 판정 JSON 만 필요하면 다시 만들면 된다
output/
state/

# 🔴 원본 수집본은 커밋하지 않는다 — 25MB 가 넘고, 제출에는 슬림본만 쓴다.
#    원본이 필요하면 collect.py 로 다시 받는다(재현 절차가 SKILL.md 에 있다).
sample-data/retail.json
sample-data/wholesale.json
sample-data/origin.json
sample-data/origin_history.json.gz
sample-data/*_PARTIAL*

__pycache__/
*.py[cod]
