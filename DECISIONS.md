# DECISIONS — 주요 결정

되돌리기 어렵거나, 방향을 바꾸거나, 기각한 대안이 있는 결정을 기록한다.

---

## 2026-07-20 · 구현 순서 → **부분 폐기 (2026-07-27)**
- **유효한 것:** 입출력과 Decision Card의 기본 모양을 먼저 정하고, **최소 기능으로 Skill→Agent 흐름을 끝까지 한 번 실행한 뒤 보강한다.** 첫 전체 실행이 실패하면 실패 원인이 생긴 Step으로 돌아간다.
- **근거:** Skill·Agent 연결 오류를 일찍 찾고, 두 항목의 평가 비중 50%에 먼저 대응한다. 출처: 대회 평가 기준, [Scaffolding.md](Scaffolding.md) S4.
- **🔴 폐기된 것:** ~~*"두 데이터셋의"* 흐름을 실행한다~~ — AdventureWorks·Contoso V2를 버렸으므로 **두 데이터셋 어댑터를 만들지 않는다.** 아래 *「공공데이터 4종으로 교체」* 의 소스 구성을 따른다. 원칙(최소 흐름을 끝까지 먼저)만 유지한다.

---

## 2026-07-27 · 수요 반응을 예측하지 않는다
- **결정:** 가격 변화에 대한 수량 반응을 **추정하지 않는다.** 대신 *"수량이 얼마나 빠지면 이 권고가 무의미해지는가"*를 역산하고, 그 값을 과거 관측 변동폭과 비교해 제시한다.
- **근거:** 당시 데이터에서 가격-수량 관계를 신뢰할 수 없었다(상관 0.048 / −0.091, 표본 부족, 성장 추세와 사업 확장이라는 교란 요인). 계량 모형으로 내생성을 다루면 *"5분 안에 설명"* 제약에 걸린다.
- **기각한 대안:** 수요 탄력성 추정 — 데이터가 뒷받침하지 못하고 설명 난이도가 높다.
- **⚠️ 근거 데이터 교체됨(2026-07-27):** 실측 근거가 된 두 데이터는 더 이상 쓰지 않는다. **결정 자체는 유지하되, 새 데이터에서 다시 검증해야 한다.**

## ~~2026-07-27 · 데이터셋 추가 여지를 닫지 않는다~~ → **폐기**
- **결정:** ~~지금은 2종(AdventureWorks · Contoso V2)으로 진행하되, 완전 연도 비교 조합 부족을 해결하기 위한 추가 탐색 여지를 남긴다.~~
- **폐기 사유(2026-07-27):** 탐색 여지를 남기는 데 그치지 않고 **데이터를 통째로 교체했다.** 아래 *「공공데이터 4종으로 교체」* 로 대체된다.
- **남기는 이유:** 부분 연도 비교를 거부한 판단은 유효했고, 그 기준이 새 데이터의 6번 조건 판정에 그대로 쓰였다.

## ~~2026-07-27 · 측정 규칙 6건 확정~~ → **부분 폐기**
- **결정:** ~~① 완전 연도끼리만 비교 ② 기준통화 단가 사용 ③ 가격은 실현가, 할인은 별도 요인 ④ 전가율은 금액 기준 `Δ단가 ÷ Δ단위원가` ⑤ 이상치 필터 없이 산출 불가 구간을 건수와 함께 표기 ⑥ 세그먼트 기본 단위는 서브카테고리~~
- **유효한 것:** ①(완전 기간 비교) · ⑤(이상치 제외 시 규칙과 건수 병기) — 데이터와 무관한 측정 원칙이라 새 데이터에도 적용한다.
- **폐기된 것:** ②③④⑥ — 통화 환산·매출할인·제조원가 전가율·서브카테고리는 **전부 이전 데이터의 스키마에 묶인 규칙**이다. 새 데이터에는 해당 컬럼이 없다.
- **함께 폐기:** *"AW 원가 시점 매칭이 정해지기 전에는 재측정을 시작할 수 없다"* 는 제약 — AW를 쓰지 않으므로 소멸했다.

---

## 2026-07-27 · 결정 유형을 「조달」로 좁힌다
- **결정:** Agent가 지원하는 CEO 의사결정을 **조달 — *무엇을 언제 얼마에 사들일 것인가*** 하나로 확정한다.
- **근거:** 공공데이터에는 특정 기업의 매출·원가·재고가 없고 **외부 환경 데이터만 있다.** 따라서 내부 장부를 전제하는 결정은 원리적으로 풀 수 없다. 조달은 *주기적 · 근거 부족 · 되돌리기 비쌈*이라는 세 조건에 더해 **내부 장부 없이도 성립**하는 유일한 유형군에 속하고, 그중 두 포털의 데이터 밀도가 가장 높다. 상세: [DATA_CRITERIA §1](DATA_CRITERIA.md)
- **기각한 대안:** 출하 시점 · 입지 선정 · 판로 선택 · 품목 구성 — 네 조건은 통과하나 받을 수 있는 데이터가 조달보다 얇다.

## 2026-07-27 · 데이터를 공공데이터로 교체한다 → **2026-07-28 13종으로 확장**
- **결정:** AdventureWorks · Contoso V2를 **버리고** 공공데이터포털 API로 간다. 처음 4종으로 시작했고, **2026-07-28에 9종을 추가해 총 13종**이 됐다. 전부 `serviceKey` 하나로 호출되며 **캐시로 두는 소스는 없다.** 현재 구성은 [DATA_SOURCES.md](DATA_SOURCES.md).
- **확장분(2026-07-28):** 등락률·연월별 통계·최근일자·**산지공판장**·지역별·기간별(중도매/소매)·친환경·가격추이. 특히 **산지공판장이 유통 3단계 분석을 가능하게 했고**, 등락률·표준편차가 API로 제공돼 직접 계산할 필요가 없어졌다.
- **근거:** 이전 두 데이터는 *최신성 · 갱신 주기 · 외부 결합*에서 막혔다. 갱신되지 않으므로 *"새 데이터 감지"* 트리거를 인위적으로 만들어야 했고, 가상 기업이라 어떤 공공데이터와도 결합되지 않았다. 새 4종은 전부 API로 갱신되고, **배추 한 품목에 대해 세 소스의 3자 대응을 실제 응답으로 입증했다.** 실측 근거: [DATA_CRITERIA §2](DATA_CRITERIA.md)
- **🔴 결합은 자동으로 되지 않는다:** 세 소스의 품목 체계가 서로 다르다(자체 품목코드 · HS코드 · 작물 특성 ID). **대응 키는 「품목 + 계절(품종)」이며 사람이 만든 대응표가 필요하다.** 문자열 매칭은 실제로 **양배추를 배추로 잡았다.** 지금 검증된 대응은 배추 하나뿐이고, 나머지는 수집 대상을 좁힌 뒤 품목마다 실제 응답으로 검증한다.
- **기각한 대안:** 공영도매시장 **실시간 경매정보** — 거래 라인 단위로 가장 두꺼우나 **연속된 완전 연도가 하나도 없다**(§8.1). KAMIS·OpenDART·KREI OASIS — 두 포털 밖이거나 관측 단위가 맞지 않는다.

## 2026-07-27 · 데이터 조건에 「재현 가능성」을 7번째로 추가한다
- **결정:** 소스 판정 기준을 6조건에서 **7조건**으로 늘린다. 새 조건은 ***제3자가 자기 인증키로 같은 데이터를 받을 수 있는가***. 이를 통과하지 못하면 본체로 쓰지 않는다.
- **근거:** 농림축산식품 포털은 **신청 시 등록한 IP에서만** 응답한다. 배점 절반이 동작에 걸려 있고 1차 심사가 *"스킬 동작 여부"*를 보는데, **심사 환경에서 멈추는 Agent는 동작하지 않는 Agent다.** 이 조건이 없어서 IP 등록 방식 API를 붙잡고 반나절을 썼다.
- **적용 결과:** 처음에는 정산정보를 IP 등록 전용으로 보고 캐시로 강등했으나, **공공데이터포털 자체 엔드포인트(`B552845/katSale/trades`)를 확인해 보조로 되돌렸다.** 같은 날 같은 시장 건수가 6,160으로 정확히 일치해 동일 데이터임을 확인했고, 오히려 **공공데이터포털 경로가 더 길다**(2018~ vs 2020~). **결과적으로 네 소스 모두 `serviceKey` 하나로 호출되며 캐시로 강등할 소스는 없다.**
- **부수 규칙:** *"양쪽 포털에 다 있다"* 를 검색 결과로 판단하지 않는다. **상세 페이지의 End Point 표기**를 본다. 이 확인을 건너뛴 것이 오판의 원인이었다.

---

## 2026-07-27 · 갈래마다 「데이터가 답하는 수위」를 명시하고 그 위로 올리지 않는다
- **결정:** 조달 세 갈래를 유지하되 각 갈래가 답하는 수위를 낮춘다. **「무엇을」= 선별**(어떤 품목이 평소와 다른가) · **「얼마에」= 가격 타당성**(같은 등급·시장 기준에서 어디쯤인가) · **「언제」= 공급위험 모니터링**(공급 쪽에서 무엇이 평소와 다른가). 특히 **「언제」를 *"지금 사라/기다려라"* 로 쓰지 않는다.**
- **근거:** 기상·수입이 가격을 **선행한다는 것을 측정하지 않았다.** 재지 않은 선행성으로 매입 시점을 권고하면 *「수요 반응을 예측하지 않는다」* 와 같은 종류의 오류가 된다. 예측으로 올리려면 예측 대상·판단 시계·기준모형을 고정하고 과거 시점 holdout에서 추가 예측력을 확인해야 한다.
- **기각한 대안:** **「무엇을」 갈래를 통째로 제외** — *"이 품목을 계속 취급할까"* 는 내부 수익성이 필요해 답할 수 없지만, **선별 층은 외부 데이터만으로 성립한다.** 그리고 136품목 중 무엇을 볼지 정해지지 않으면 **자동 실행 자체가 시작되지 않는다.** 빼면 루프가 성립하지 않는다.

## 2026-07-27 · 산출물을 2층으로 나눠 입력 없이도 완결되게 한다
- **결정:** **1층(항상 산출)** — 공공데이터만으로 시장 기준가격·분포·전기 대비 변화·지역 격차·공급위험 신호를 내고 여기서 주목 품목을 선별한다. **2층(입력이 있을 때만)** — 사용자 매입가를 얹어 시장 분포상 위치를 판정한다. 입력이 없으면 2층 항목만 **「미판정(입력 없음)」**으로 표시하고 루프는 계속 돈다.
- **근거:** 대회 요건이 *"사람 개입 없이 반복 동작"* 이다. 직전 설계는 매입가 입력이 없으면 Agent가 아무 산출물도 내지 못하는 구조였고, 그대로 가면 **1차 심사의 *스킬 동작 여부* 에서 걸린다.** 매입가는 판단을 개인화하는 재료이지 실행 조건이 아니다.
- **기각한 대안:** 입력을 필수로 두고 없으면 안내 메시지를 반환 — 사람 입력을 기다리는 설계로 읽힌다.

---

## 반대 의견

AI가 반박했으나 사람의 판단으로 다르게 진행한 건을 남긴다.
나중에 *"그때 이런 우려가 있었다"*를 추적해 판단의 질을 검증하기 위한 기록이며, 번복을 요구하는 것이 아니다.

| 일자 | 사안 | 제기된 우려 | 실제 결정 |
|---|---|---|---|
| 2026-07-25 | 문서 경량화와 KPI 확정(D1) 중 무엇을 먼저 할지 | D1이 비면 D2·D5·D6·D7·D8이 모두 대기 상태가 되어 Step 1~4가 막힌다. 킥오프에 KPI가 미정이면 1.5일 실전에서 결정과 구현을 동시에 하게 되고, 이는 *"구현 중 새 결정 금지"* 원칙을 스스로 어기는 것이다 | **문서 경량화를 먼저 한다.** 근거 — 인지 부채는 임계점을 넘으면 회복이 어렵고, 사용자가 맥락을 잃으면 *"이해하지 못한 산출물은 실패한 산출물"* 이라는 상위 제약에 걸린다. 일정 지연은 회복 가능한 손해다 |
