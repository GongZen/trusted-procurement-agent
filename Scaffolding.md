# Scaffolding — 프로젝트 진행 구조

> 작성 2026-07-20 · 개정 2026-07-25
> 목적: **완성물까지 어떤 단계로 도달할지** 정의한다.
> 범위 밖: 제품·설계 내용은 [PROJECT_BRIEF.md](PROJECT_BRIEF.md), 협업 절차는 [COLLABORATION.md](COLLABORATION.md).
> 쉽고 직관적으로, 짧게 쓴다.

---

## 0. 완성물

1. **Skill**
   - AdventureWorks와 Contoso V2에서 같은 입력·출력 형식으로 동작한다.
   - 두 데이터 변환기가 공통 테스트를 통과한다.
   - 데이터셋별 검증 보고서(`ValidationReport`)를 만든다.
2. **Agent**
   - 새 데이터나 정해진 시간을 감지해 Skill을 실행한다.
   - 이전 결과와 비교하고 새 결과를 저장한다.
   - 실패하면 재시도하고, 사람의 승인이 필요한 사안은 자동 확정하지 않는다.
3. **발표자료 · 포스터 · 발표 리허설**

> **Skill과 Agent의 역할·평가 증거를 분리하는 것**만 먼저 확정하고, 폴더 구조와 제출 형식은 킥오프 자료를 확인한 뒤 정한다. 평가는 두 항목을 따로 보지만 기술 형식은 아직 공개되지 않았다.

---

## 1. 확정한 결정

| # | 결정 | 내용 | 근거 |
|---|---|---|---|
| S1 | 사전 제작 | 공통 분석 코드·테스트·문서는 지금 만든다. 제출 형식에 맞추는 작업은 킥오프 뒤에 한다 | 공지에 사전 제작 제한이 없음 |
| S2 | 기술 구조 | 플랫폼과 분리된 Python 코어를 만들고, JSON·Markdown으로 데이터를 주고받는다. 플랫폼 연결부는 나중에 교체한다 | 제출 형식이 아직 공개되지 않음 |
| S3 | 데이터 범위 | Skill 하나를 AdventureWorks와 Contoso V2 **두 곳에만** 적용한다 | [DATA_SOURCES.md](DATA_SOURCES.md) |
| S4 | 진행 순서 | 입출력과 Decision Card의 기본 모양을 먼저 정한 뒤, 최소 기능으로 끝까지 한 번 실행한다. 이후 각 기능을 보강한다 | 연결 오류를 일찍 발견하기 위함 |

코드는 다음 네 부분으로 나눈다.

1. **데이터 변환** — 두 데이터셋을 공통 형식으로 바꾼다.
2. **분석** — 검증, 계산, 가설 점수, Decision Card 생성을 맡는다.
3. **LLM 연결** — Solar를 호출한다. API가 멈춰도 숫자 분석은 완료되어야 한다.
4. **플랫폼 연결** — 실행 조건, 일정, 파일 입출력, 비밀키를 관리한다.

데이터 교환용 JSON에는 버전 번호(`schema_version`)를 넣는다.

> **LLM의 역할** — 숫자 계산은 Python으로 고정한다. Solar는 **근거가 있는 가설 비교와 권고 작성**에 사용하되, 산출물에 출처를 붙이고 **숫자를 새로 만들거나 바꾸지 못하게** 막는다. 설명 문장 작성으로만 제한할 필요는 없다. 근거: 수치 신뢰성을 지키면서도 LLM을 활용한 분석 과정을 보여줄 수 있다.

---

## 2. 단계 구성

| Step | 해야 할 일 | 산출물 | 통과 기준 |
|---|---|---|---|
| **0. 설계 확정** | KPI, 분석 단위, 원가 기준, 비교 기간, 할인·반품 처리, 교차항 배분, 경계 산정 근거를 정한다 | 분석 사양서 · `DECISIONS.md` | 미정 항목이 없고 7개 결정에 근거가 있음 |
| **1. 데이터 준비** | 두 데이터 변환기와 공통 데이터 검사를 만든다 | 분석 테이블 · 검증 보고서 2개 | 두 변환기가 공통 테스트를 통과하고 모든 누락·불일치가 보고됨 |
| **2. 숫자 분석** | 마진 변동을 가격·수량·믹스·원가·조정으로 나눈다 | 마진 분해 모듈 | 같은 입력은 같은 결과를 내며, 원인별 합계와 전체 변동액의 차이가 허용 오차 안에 있음 |
| **3. 가설 평가** | 원가·가격·믹스 가설의 지지·반박 근거를 점수화한다 | `HypothesisScore` | 계산법이 기록되고, 같은 입력은 같은 점수와 미확인 항목을 냄 |
| **4. Skill 완성** | Step 1~3을 한 번의 명령으로 실행해 Decision Card를 만든다 | Skill · 입출력 설명 · 예시 · 테스트 | 두 데이터셋이 같은 출력 형식을 만들고 모든 숫자에 근거 번호가 있음 |
| **5. Agent 완성** | 실행 감지, Skill 호출, 이전 결과 비교, 저장, 재시도, 사람 확인 요청을 연결한다 | 반복 실행 Agent · 상태 · 승인 규칙 | 새 데이터는 자동 분석하고, 같은 데이터는 중복 처리하지 않으며, 위험한 결정은 자동 확정하지 않음 |
| **6. 비즈니스 문서** | Decision Card를 1페이지 문서로 바꾼다 | CEO Decision Card · 브리핑 | 공개 출처가 있는 문서 형식을 사용하고, 한 페이지와 필수 항목 조건을 지킴 |
| **7. 전체 테스트** | 두 데이터셋, 새 환경, 반복 실행, 재시작, 잘못된 입력, API 오류, 실행시간을 확인한다 | 테스트 결과 | 모든 테스트를 통과하고, LLM 오류 시에도 숫자 결과가 남음 |
| **8. 제출 포장** | 제출 형식 적용, 발표자료·포스터 작성, 발표 연습, README 실행 확인 | 제출물 | 제출 조건을 모두 충족하고 발표 리허설을 1회 이상 마침 |

첫 번째 전체 실행의 목표는 다음과 같다.

> **같은 요청을 두 공개 데이터에 입력 → 검증 보고서와 간단한 Decision Card 생성 → Agent가 새 정보를 감지해 각각 한 번 다시 판단**

근거: Skill과 Agent가 전체 점수의 50%이므로 두 기능의 연결을 먼저 검증해야 한다.

---

## 3. 실패하면 돌아갈 단계

| 발견한 문제 | 돌아갈 단계 |
|---|---|
| 미정 사항이 남거나 정의가 계속 바뀜 | Step 0 |
| 데이터 변환 공통 테스트 실패, 출처·누락 보고 부족 | Step 1 |
| 원인별 합계와 전체 변동액이 맞지 않음 | Step 0 또는 2 |
| 작은 입력 변화에 가설 점수가 이유 없이 크게 바뀜 | Step 3 |
| 숫자의 근거 번호가 빠짐 | Step 2 또는 4 |
| 중복 실행, 재시작 실패, 승인 대상 자동 확정 | Step 5 |
| 한 페이지 초과, 필수 항목 누락 | Step 6 |
| 새 환경 실행·오류 복구·실행시간 테스트 실패 | 실패 원인이 생긴 Step |
| README 실행 실패, 제출 형식·링크 누락 | Step 8 |

구현할 때 각 Step에 실제 확인 명령과 기대 결과를 기록한다. 근거: 평가 기준이 재현성과 사람 없는 반복 실행을 본다.

---

## 4. 아직 정하지 않은 사항

1. 사전 제작 허용 범위 — 킥오프 교육자료 확인 후 결정
2. Agent와 Skill의 최종 제출 형식 — 킥오프 자료 확인 후 결정
3. CEO Decision Card의 공개 문서 사례 — Step 6 전에 조사 후 결정
4. 일정과 시간 배분

---

## 5. 작업 관리

각 Step은 **GitHub Issue 하나**로 등록하고, 위 "통과 기준"을 Issue의 완료 조건으로 옮겨 적는다.
구현은 Issue별 브랜치에서 진행하고 PR로 병합한다. 절차는 [COLLABORATION.md](COLLABORATION.md) 참조.
