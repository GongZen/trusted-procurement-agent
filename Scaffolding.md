# Scaffolding — 프로젝트 진행 구조

> 작성 2026-07-20 · **개정 2026-07-28** (주제 전환에 따라 결정 목록 재설계)
> 목적: **완성물까지 어떤 단계로 도달할지** 정의한다.
> 문제 정의와 설계 근거는 [README.md](README.md), 데이터 판정은 [DATA_CRITERIA.md](DATA_CRITERIA.md), 협업 절차는 [COLLABORATION.md](COLLABORATION.md).

---

## 🔴 이 문서의 현재 상태

**§2의 결정 목록이 비어 있다.** 이전 목록(D1~D15)은 *수익성 진단 Agent + 완결 데이터셋 2종*을 전제한 것이어서, 주제를 **조달 의사결정 지원**으로 전환하면서 대부분 무효가 됐다([README §8](README.md)).

**비운 것은 의도적이다.** 새 결정을 급하게 채우면 잠금의 목적(구현 중 판단을 만들지 않기)이 무너진다. 아래 §2에 **결정이 필요한 영역**만 남겼고, 번호·내용·확정 조건은 **Skill 입출력 설계 작업에서 확정한다.**

이미 확정된 결정은 [DECISIONS.md](DECISIONS.md)에 있다 — 결정 유형(조달) · 데이터 소스 4종 · 세 갈래의 답하는 수위 · 산출물 2층 구조 · 재현 가능성 조건.

---

## 이 문서의 원칙

> ### 🔒 구현 중에는 새 결정을 만들지 않는다.
> 결정은 **Step 0에서 잠근다.** 구현 도중 결정이 필요해지면
> 그 자리에서 판단하지 않고 **Step 0으로 돌아가 잠근 뒤 재개**한다.
>
> **예외는 하나뿐이다** — 플랫폼 연결 결정은 타임리 사양을 확인해야 정할 수 있으므로
> **플랫폼 연결 Step 착수 전**까지 잠근다.

**왜 이 원칙이 필요한가** — 실제 제작 구간이 짧다. 그 시간에 *"이건 어떻게 하지?"*가 나오면 구현이 멈춘다. 결정과 구현을 시간적으로 분리하는 것이 이 프로젝트의 가장 큰 일정 리스크 대응이다.

그래서 각 Step에 **"잠겨 있어야 할 선행 결정"**을 명시한다. 하나라도 비어 있으면 그 Step을 시작하지 않는다.

---

## 0. 완성물

1. **동작하는 Skill**
   - 공공데이터 API에서 수집 → 검증 → 기준가 산출 → 이상 신호 탐지 → 카드 생성이 **한 번의 실행으로** 이어진다.
   - **같은 입력이면 같은 결과**가 나온다.
   - 검증 보고서를 함께 낸다 — 미조사일·미매칭 품목·수집 실패를 숨기지 않는다.
2. **Data Agent**
   - 트리거(새 시세 갱신 또는 일정)를 감지해 Skill을 실행한다.
   - 이전 결과와 비교하고 새 결과를 저장한다.
   - **사람이 없어도 루프가 끊기지 않는다.** 실패는 재시도로 흡수한다.
   - **사용자 매입가 입력이 없어도 1층 산출물이 완주한다**([README §4](README.md)).
3. **발표자료 · 포스터**

> 🔴 **제출물의 상세 형식(파일 형태·개수·분량)을 확인하고 Step 8에 반영한다.**
> 1차 심사가 *"필수 제출물 여부"*를 기계적으로 스크리닝하므로 **형식 누락은 곧 탈락**이다.

---

## 1. 확정한 결정 (프로젝트 운영)

| # | 결정 | 내용 | 근거 |
|---|---|---|---|
| S1 | 사전 제작 | 공통 분석 코드·테스트·문서는 미리 만든다. 제출 형식에 맞추는 작업은 킥오프 뒤에 한다 | 공지에 사전 제작 제한이 없음 |
| S2 | 기술 구조 | 플랫폼과 분리된 Python 코어를 만들고, JSON·Markdown으로 데이터를 주고받는다. 플랫폼 연결부는 교체 가능하게 얇게 만든다 | 실행 플랫폼이 **타임리**로 지정돼 있으나 세부 사양이 미공개 |
| S3 | **데이터 범위** | **공공데이터포털 API 4종.** 본체는 「일별 도·소매 가격정보」, 보조는 정산정보·주산지 날씨·수출입실적 | [DATA_SOURCES.md](DATA_SOURCES.md) · [DATA_CRITERIA.md](DATA_CRITERIA.md) |
| S4 | 진행 순서 | 입출력의 기본 모양을 먼저 정한 뒤, **최소 기능으로 끝까지 한 번 실행한다.** 이후 각 기능을 보강한다 | 연결 오류를 일찍 발견하기 위함 |
| S5 | 결정·구현 분리 | 구현 중 새 결정을 만들지 않는다 (위 원칙) | 실제 제작 구간이 짧다 |
| **S6** | **수집 범위 제약** | 전 품목 3년치를 하루에 수집할 수 없다(일 트래픽 10,000). **품목 수·기간을 좁히거나 여러 날에 나눈다** | 실측: [DATA_CRITERIA §2.2](DATA_CRITERIA.md) |

코드는 네 부분으로 나눈다.

1. **수집** — 네 API에서 받아오고 페이지네이션·재시도·캐시를 처리한다.
2. **분석** — 검증, 기준가·분포 산출, 이상 신호 탐지, 카드 생성.
3. **LLM 연결** — Solar 호출. **API가 멈춰도 숫자 분석은 완료되어야 한다.**
4. **플랫폼 연결** — 트리거, 일정, 파일 입출력, 비밀키.

데이터 교환용 JSON에는 버전 번호(`schema_version`)를 넣는다.

> **LLM의 역할** — 숫자 계산은 Python으로 고정한다. Solar는 **근거가 있는 해석과 권고 작성**에 사용하되, 출처를 붙이고 **숫자를 새로 만들거나 바꾸지 못하게** 막는다.

---

## 2. 🔒 Step 0 — 잠가야 할 결정

**이전 목록(D1~D15)은 폐기했다.** 아래는 **결정이 필요한 영역**이며, 번호·정확한 질문·확정 조건은 이 Step에서 만든다.

| 영역 | 정해야 하는 것 | 왜 필요한가 |
|---|---|---|
| **수집 범위** | 지원 품목 목록과 기간. **일 트래픽 10,000 안에 들어오는 조합** | S6 제약. 이것이 안 정해지면 수집기를 만들 수 없다 |
| **품목 대응표** | 세 소스의 품목 체계를 잇는 대응 규칙과 **완료 조건** | 지금 검증된 대응은 배추 하나뿐이다([DATA_CRITERIA §5](DATA_CRITERIA.md)) |
| **기준가 정의** | *"평소"*를 무엇으로 정의하나 — 평년 동기 · 직전 N일 · 등급별 분포 중 무엇 | 「얼마에」 갈래의 판정 근거 |
| **이상 신호 기준** | *"평소와 다르다"*를 어떤 규칙으로 판정하나. **임의 임계치를 쓰지 않는 방법** | 「무엇을」 선별의 핵심. 이것이 루프의 시작점이다 |
| **공급위험 신호 정의** | 기상·수입 물량의 무엇을 어떻게 표시하나. **예측으로 올리지 않는 경계** | 「언제」 갈래. 선행성을 측정하지 않았으므로 표시에 머물러야 한다 |
| **Skill 입출력 계약** | 단계별 JSON 필드 명세와 `schema_version` | Step 4의 통과 기준 |
| **사용자 입력 형식** | 매입가를 어떤 최소 형태로 받나. **없을 때의 산출 계약** | 2층 구조의 위층. 없어도 1층이 완주해야 한다 |
| **Agent 동작** | 트리거 조건, 재판정 규칙, 실패 재시도, 승인 표시 방식 | §4의 통과 기준 |
| **상태·추적** | 이전 결과 저장 형식, 근거 추적, 상태 버전 | 같은 입력에 같은 결과 |
| **산출물 형식** | 한 장에 담을 필수 항목과 배치 | Step 6 |
| **성능 전략** | 대용량 수집·재수집 처리 | Step 7 |
| **플랫폼 연결** | 타임리 트리거·스케줄 사용 방식 | 🔴 **사양 확인 후에만 잠글 수 있다** |

**Step 0 산출물** — 결정 목록과 각 결정의 확정 조건 + [DECISIONS.md](DECISIONS.md) 기록. 결정 하나가 잠길 때마다 추가한다.
**Step 0 통과 기준** — **다음 Step에 필요한 선행 결정**에 빈칸이 없고, 각 항목에 근거와 출처가 있음

> **결정 하나 = 근거 하나.** 임의 임계치나 관행을 근거로 쓰지 않는다.
> 데이터가 뒷받침하지 못하는 것은 **「미측정」으로 표기하고 그 상태로 진행**한다.

---

## 3. 단계 구성

각 Step은 **선행 결정이 잠긴 뒤에만** 시작한다.

| Step | 해야 할 일 | 산출물 | 통과 기준 |
|---|---|---|---|
| **0. 결정 잠금** | §2의 영역을 결정으로 번호화하고 확정한다 | 결정 목록 · `DECISIONS.md` | 다음 Step에 필요한 결정에 빈칸 없음, 각 결정에 근거 있음 |
| **1. 수집** | 네 API에서 받아오는 수집기를 만든다. 페이지네이션·재시도·캐시·트래픽 제한 | 수집 스크립트 · 원본 캐시 | 지정 범위를 전량 수집. **`totalCount`와 수신 건수 일치.** 실패가 기록됨 |
| **2. 검증** | 스키마·기간·키 중복·단위·미조사일을 확인한다 | 검증 보고서 | 결측·중복·이상값이 **건수와 함께** 보고됨. 미조사일과 결측이 구분됨 |
| **3. 기준가·분포** | 품목·등급·시장별 기준가와 분포를 산출한다 | 기준가 모듈 | 같은 입력 = 같은 결과. 단위가 정규화됨 |
| **4. 선별·신호** | 이상 신호를 탐지해 주목 품목을 고르고, 공급위험 신호를 붙인다 | 선별 모듈 | **임의 임계치가 하나도 없음.** 같은 입력 = 같은 선별 결과 |
| **5. Skill 완성** | Step 1~4를 한 번의 명령으로 실행해 산출물을 만든다 | Skill · I/O 문서 · 예시 · 테스트 | **매입가 입력 없이도 1층 산출물 완주.** 모든 숫자에 근거 추적 |
| **6a. Agent 코어** | 트리거 감지 → Skill 실행 → 이전 결과 비교 → 저장 → 알림 루프. **플랫폼 비의존** | 반복 실행 루프 · 상태 저장 | §4 참조 — **사람 개입 없이 루프 완주** |
| **6b. 플랫폼 연결** | 6a의 루프를 타임리 트리거·스케줄에 연결한다 | 플랫폼에서 동작하는 Agent | 플랫폼 위에서도 6a의 통과 기준이 그대로 성립 |
| **7. 산출물 서식** | 산출물을 한 페이지 문서로 바꾼다 | Decision Card · 브리핑 | 한 페이지 · 필수 항목 전부 충족 |
| **8. 전체 테스트** | 새 환경, 반복 실행, 재시작, 잘못된 입력, API 오류, 트래픽 초과, 실행시간 | 테스트 결과 | 전 항목 통과. **LLM 오류 시에도 숫자 결과가 남음** |
| **9. 제출 포장** | 제출 형식 적용, 발표자료·포스터, 발표 연습, README 실행 확인 | 제출물 | 제출 조건 전부 충족, 리허설 1회 이상 |

**첫 번째 전체 실행의 목표**

> 지정 품목 범위를 수집 → 검증 보고서와 간단한 산출물 생성
> → Agent가 새 시세를 감지해 한 번 다시 판단

근거: Skill(25%) + Agent(25%) = 배점의 절반. 두 기능의 **연결**을 먼저 검증해야 한다.

> ⚠️ **Step 1이 가장 먼저다.** 이전 구조에서는 데이터가 로컬 파일이라 수집이 없었다.
> 이제 수집이 실패하면 아무것도 진행되지 않으므로 **수집기가 첫 번째 vertical slice의 시작점**이다.

---

## 4. 🔴 "사람 개입 없이 반복 동작"을 어떻게 만족시키나

**문제** — 평가 기준은 *"인간 개입 없이 안정적으로 반복 동작하는가"*(Agent 25%)다.
그런데 우리 설계에는 **사용자 매입가 입력**과 *"승인이 필요한 사안은 자동 확정하지 않는다"*가 있다.
**심사자가 이를 "사람이 있어야 도는 Agent"로 읽으면 배점의 25%가 위험해진다.**

**해결 방향 — 루프와 판정을 분리하고, 산출물을 층으로 나눈다.**

| 구분 | 설계 |
|---|---|
| **실행 루프** | **완전 자동.** 트리거 감지 → 수집 → Skill 실행 → 이전 결과 비교 → 저장 → 알림까지 사람 없이 완주한다. 어떤 경우에도 사람을 기다리며 멈추지 않는다 |
| **사용자 입력** | **선택 사항.** 매입가가 없으면 1층(시장 기준가·분포·변화·공급위험)만 산출하고, 2층 항목을 **「미판정(입력 없음)」**으로 표시한다. 입력을 요구하며 멈추지 않는다 |
| **HITL** | **멈춤 장치가 아니라 산출물 안의 표시.** 산출물에 `승인 필요` 플래그와 사유를 넣는다. Agent는 그것을 표시하고 계속 돈다 |
| **실패 처리** | 재시도 → 그래도 실패하면 실패 사실을 기록하고 다음 주기로 넘어간다. **API 트래픽 초과도 실패로 기록하고 다음 주기에 이어받는다** |

> **한 문장으로:** *"Agent는 사람이 없어도 계속 돈다. 다만 사람이 결정해야 할 것을 표시한다."*

**Step 6a 통과 기준**

- [ ] 사람이 한 번도 개입하지 않은 상태로 **연속 3회 이상 트리거 → 산출 완주**
- [ ] **매입가 입력이 없는 상태에서도 1층 산출물이 완결됨**
- [ ] 같은 데이터를 다시 줘도 **중복 처리하지 않음**
- [ ] 새 데이터에는 **이전 결과와의 차이(재판정 사유)**가 산출물에 남음
- [ ] 실행 중 오류가 나도 **루프가 죽지 않음** (재시도 후 기록하고 계속)
- [ ] **API 트래픽 초과 시에도 루프가 죽지 않음** (수집 범위를 나눠 다음 주기로 이어받음)
- [ ] 승인이 필요한 사안은 **자동 확정하지 않되, 루프를 멈추지도 않음** (플래그로 표시)

**Step 6b 통과 기준** — 타임리에 얹은 뒤에도 위 7개가 **그대로 성립**할 것.
6a를 플랫폼 없이 먼저 검증하는 이유가 이것이다. 6b에서 실패하면 원인이 연결부에만 있음을 알 수 있다.

---

## 5. 실전 구간 운영

**사전에 끝내둘 것** — Step 0 ~ 6a. **플랫폼 연결 없이 완주 가능한 범위 전부.**
**당일에만 할 수 있는 것** — Step 6b(타임리 연결), 제출 형식 적용, 발표 준비.

| 시점 | 하는 일 |
|---|---|
| 킥오프 직후 | 제출물 형식 확인 → Step 9 체크리스트 확정 · 타임리 사양 확인 → 플랫폼 연결 결정 잠금 |
| 제작 구간 전반 | **Step 0(결정 잠금) → 1 → 2 → 3 → 4 → 5**까지. 여유가 있으면 6a |
| 제작 구간 후반 | **Step 6b**(타임리 연결) → 7 → 8 |
| 마감 직전 | 발표자료·포스터 마무리 (Step 9) |

> 이 구분이 무너지면 제작 구간 안에 완성할 수 없다.

---

## 6. 실패하면 돌아갈 단계

| 발견한 문제 | 돌아갈 단계 |
|---|---|
| **구현 중에 새 결정이 필요해짐** | **Step 0** (잠그고 재개) |
| 미정 사항이 남거나 정의가 계속 바뀜 | Step 0 |
| 수집 건수가 `totalCount`와 다름, 트래픽 초과로 중단 | Step 1 |
| 결측·중복·이상값이 보고되지 않음 | Step 2 |
| 단위가 섞여 비교가 성립하지 않음 | Step 0(기준가 정의) 또는 3 |
| 작은 입력 변화에 선별 결과가 이유 없이 크게 바뀜 | Step 0(이상 신호 기준) 또는 4 |
| 숫자의 근거 추적이 빠짐 | Step 3 또는 5 |
| **매입가 없이 실행했을 때 산출물이 나오지 않음** | Step 0(사용자 입력 형식) 또는 5 |
| 중복 실행, 재시작 실패, 루프가 사람을 기다리며 멈춤 | Step 6a |
| 플랫폼에 얹은 뒤에만 실패함 | Step 6b (코어는 건드리지 않는다) |
| 한 페이지 초과, 필수 항목 누락 | Step 0(산출물 형식) 또는 7 |
| 새 환경 실행·오류 복구·실행시간 테스트 실패 | 실패 원인이 생긴 Step |
| README 실행 실패, 제출 형식·링크 누락 | Step 9 |

각 Step에 실제 확인 명령과 기대 결과를 기록한다. 근거: 평가 기준이 **재현성과 사람 없는 반복 실행**을 본다.

---

## 7. 아직 정하지 않은 사항

**여기에는 "우리가 정할 것"을 두지 않는다.** 우리가 정할 것은 전부 §2의 결정 영역에서 번호화한다.
아래는 **외부에서 공개되기를 기다려야 하는 확인 사항**뿐이다.

1. **필수 제출물의 상세 형식** — 🔴 최우선
2. **타임리 플랫폼의 트리거·스케줄 사양** — 확인 후 플랫폼 연결 결정으로 잠근다
3. 사전 제작 허용 범위의 세부 해석

---

## 8. 작업 관리

- 각 Step은 **GitHub Issue 하나**로 등록하고, 위 "통과 기준"을 Issue의 완료 조건으로 그대로 옮긴다.
- **Step 0의 결정은 각각 별도 Issue**로 만든다. 결정 하나 = Issue 하나 = PR 하나여야 근거가 추적된다.
- 구현은 Issue별 브랜치에서 진행하고 PR로 병합한다. 절차: [COLLABORATION.md](COLLABORATION.md)
