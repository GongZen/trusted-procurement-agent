# 수정 계획 — PR #14 blocker 7건

> 작성 2026-07-28 · **구현 전에 검토받는다.**
> 근거: [PR #14 검토](https://github.com/GongZen/trusted-procurement-agent/pull/14)
> 마감까지 약 16시간. **틀린 것을 빨리 고치는 것보다 맞게 고치는 것이 중요하다.**

---

## 순서를 정한 기준

**의존 관계가 있는 것을 먼저 한다.** ④(전역 상태)를 고치면 단위가 버킷으로 들어가고,
그래야 ⑥(라벨과 숫자 불일치)에서 단위별로 비교할 수 있다.

```
A. 결정성 회복      ④ 전역 상태 제거
B. 정직성 회복      ⑥ 라벨-숫자 불일치 · ② 부분 응답
C. 자료 무결성      ① 경로 우선순위 · ③ 지문
D. 루프 독립성      ⑦ 사람 대기 제거
E. 임계치 정리      ⑤
```

---

## A. ④ 전역 상태 제거 — 같은 입력에 같은 결과

### 지금 무엇이 잘못됐나

```python
단위기억: dict[tuple, str] = {}          # 모듈 전역
같은달_관측()  → 단위기억을 채운다
품목별_집계()  → 단위기억을 몰래 읽는다
```

**같은 무단위 입력이 새 프로세스에서는 `""`, 앞선 호출 뒤에는 `"10kg"` 이 된다.**
[Scaffolding §4](Scaffolding.md)의 *「같은 입력에 다른 결과가 나오는 것」* — 항상 blocker다.

### 어떻게 고치나

`같은달_관측()` 의 반환 형태를 바꾼다. 값만 담던 것을 **단위와 함께** 담는다.

```python
# 전
버킷[키] = [("2021", 4500.0), ("2022", 5100.0)]

# 후
버킷[키] = {"관측": [("2021", 4500.0), ...], "단위": "10kg"}
```

`품목별_집계()` 는 이 구조에서 단위를 읽는다. **전역 변수를 지운다.**

### 통과 기준

새 프로세스에서 두 번 돌린 결과 JSON 이 **바이트 단위로 같아야** 한다
(생성시각 제외). 시험으로 고정한다.

---

## B. ⑥ 라벨과 숫자가 반대로 말하는 것

### 지금 무엇이 잘못됐나

```
사과 도매  70,644 → 74,006원 (+5%)      ← 평년보다 높다
산출물     "도매·소매가 평년보다 낮습니다"  ← 낮다고 말한다
```

임계치를 걷어내며 라벨을 **순위**로 바꿨는데, `단계해석()` 은 그 라벨을
**평균 대비 높낮이**로 읽는다. **순위 하위권과 평균 이하는 다른 말이다.**

과거 관측이 {100, 200, 300, 400, 500} 이고 올해가 350 이면
순위는 3/6(하위권)이지만 평균(300)보다 **높다.**

### 어떻게 고치나

**두 축을 분리해 각각 이름을 준다.** 하나로 뭉치려 한 것이 원인이다.

```python
@dataclass
class 단계판정:
    자리: str      # 순위 축 — "최근 6년 중 1번째" · "6년 중 4번째"
    대비: str      # 평균 축 — "평년보다 높음" · "평년 수준" · "평년보다 낮음"
```

- **`자리`** 는 순위에서만 나온다. 임계치 없음.
- **`대비`** 는 평년평균 대비 증감률의 **부호**에서만 나온다. 임계치 없음.
  (0% 는 「평년 수준」. 부호는 데이터가 정한다)

`단계해석()` 은 **`대비` 만 읽는다** — 「어디서 벌어졌나」는 평균 대비 이야기이기 때문이다.
`자리` 는 화면에 함께 표시하되 해석에 쓰지 않는다.

### 함께 고칠 것 — 「평년 수준 품종」

```
"애호박 품종은 평년 수준입니다"   ← 실제로는 +15%, 3/6위
```

**「최고가가 아니다」를 「평년 수준이다」로 바꿔 말했다.** 최고가 여부와
평년 대비는 다른 축이다. **평년 대비 부호로만 판정**하도록 바꾼다.

### 통과 기준

증감률 부호와 문장의 방향이 **모든 품목에서 일치**해야 한다. 시험으로 고정한다.

---

## B. ② 부분 응답이 성공으로 계산되는 것

### 지금 무엇이 잘못됐나

```python
first.error = f"{page}페이지에서 중단: {nxt.error}"
return first          # ok 는 여전히 True
```

`total=1500, 수신=1000, error="2페이지 429"` 인데 `ok=True` 다.
`collect.py` 가 이를 행에 합치고 종료코드 0으로 끝내며, `agent.py` 는 **성공 지문을 저장한다.**
→ 그 자료는 영영 다시 받지 않는다.

**어제 산지 과거에서 겪은 「반쪽 파일」과 같은 종류다.** 파일 층에서는 막았는데
호출 층에서는 안 막혀 있었다.

### 어떻게 고치나

```python
if 수신 != total or 중단됨:
    return CallResult(op, ok=False, rows=rows, total=total,
                      error="부분 응답: totalCount=1500 수신=1000")
```

**부분 응답은 실패다.** 받은 행은 함께 돌려주되 `ok=False` 로 표시한다.
`collect.py` 는 `ok=False` 인 소스를 **저장하지 않고** 기존 파일을 유지한다.
`agent.py` 는 지문을 갱신하지 않아 다음 주기에 다시 시도한다.

### 🔴 판단이 필요한 지점

`recent/price` 처럼 **매번 totalCount 가 달라지는** 엔드포인트가 있으면
이 규칙이 과하게 걸릴 수 있다. **첫 페이지만 쓰는 호출(`rows` 지정)은 예외**로 둔다.

---

## C. ① 경로 우선순위 — 오늘 자료와 어제 스냅샷이 섞이는 것

### 지금 무엇이 잘못됐나

```python
for 후보이름 in (f"{이름}_slim", 이름):        # 이름을 먼저 순회
    for base in (SAMPLE, SKILL / "sample-data"):  # 위치를 나중에
```

**새로 수집한 `TPA_DATA_DIR/retail.json` 보다 동봉 `retail_slim.json.gz` 가 이긴다.**
실제로 기준일 20260728·산지 0행을 수집했는데 결과에는 동봉본의 산지 값이 나왔다.

### 어떻게 고치나

**순회 순서를 뒤집는다.** 위치가 먼저다.

```python
for base in (SAMPLE, SKILL / "sample-data"):      # 위치 먼저
    for 후보이름 in (f"{이름}_slim", 이름):
```

한 위치 안에서만 슬림본을 우선한다. **다른 위치의 슬림본이 이 위치의 원본을 이기지 않는다.**

### 함께 고칠 것

수집본이 어디서 왔는지 **산출물에 남긴다.** 「동봉 스냅샷」인지 「방금 수집」인지
심사자가 화면에서 구분할 수 있어야 한다.

---

## C. ③ 지문이 지역을 구분하지 못하는 것

### 지금 무엇이 잘못됐나

```python
f"{exmn_ymd}|{ctgry_cd}|{item_cd}|{vrty_cd}|{grd_cd}|{se_cd}|{exmn_dd_prc}"
```

**`sgg_cd`(시군구)·`mrkt_cd`(시장)가 빠졌다.** 서울과 부산의 가격이 맞바뀌어도
정렬 후 해시가 같아 **같은 지문**이 된다.

### 어떻게 고치나

**판정에 쓰는 식별 열을 전부 넣는다.**

```python
f"{exmn_ymd}|{ctgry_cd}|{item_cd}|{vrty_cd}|{grd_cd}|{se_cd}"
f"|{sgg_cd}|{mrkt_cd}|{exmn_dd_prc}"
```

### 함께 고칠 것 — 키 없는 분기에도 지문을 만든다

```python
except ApiKeyMissing:
    됨, 문제 = 파이프라인실행(수집함=False)   # 지문 없이 매번 실행
```

**동봉 스냅샷의 지문**을 만들어 상태에 저장한다. 스냅샷이 안 바뀌면 두 번째
실행부터 「변화 없음」이 나와야 한다. **지금은 매번 「새 자료 처리」다.**

---

## D. ⑦ 예약 실행이 사람을 기다리는 것

### 지금 무엇이 잘못됐나

`AGENT.md` 의 `trigger_prompt` 첫 줄:

```
1. 환경변수 DATAGO_KEY 가 없으면 request_env_var 로 요청한다.
```

**예약 실행(매일 06:00)에서 이 요청이 사람 응답을 기다리면 루프가 멈춘다.**
Python 코어는 바로 스냅샷으로 완주하는데 **플랫폼 래퍼가 멈춘다.**
[Scaffolding §4](Scaffolding.md)의 *「사람을 기다리며 멈추는 설계」* — 항상 blocker다.

### 어떻게 고치나

**키 요청을 예약 루프에서 완전히 뺀다.**

```
예약 실행    키가 있으면 쓰고, 없으면 곧바로 스냅샷으로 완주한다. 묻지 않는다
최초 설정    사용자가 대화로 부를 때만 키를 요청한다 (별도 절차)
```

`AGENT.md` 의 `trigger_prompt` 를 다시 쓰고, **키 요청 단계를 SKILL.md 의
「준비」 절로 옮긴다.**

---

## E. ⑤ 임계치 정리

### 검토가 지적한 것

| 위치 | 값 | 무엇을 가르나 |
|---|---|---|
| `analyze.품목별_집계` | `최소관측수 3` | 판정 대상 여부 |
| `config` | `우선검토_품목수 5` | 알림 대상 여부 |
| `analyze.품목별_집계` | `순위/전체 <= 0.1` | 상위10퍼센트구간 |
| `analyze.사람말로` | `도시들[:3]` | 몇 개를 이름으로 부를지 |
| `render.그래프` | `len(값) < 3` | 그래프를 그릴지 |

**내 자체 판정(「범위이지 임계치가 아니다」)은 틀렸다.** 값을 바꾸면 대상이 달라진다.

### 어떻게 고치나 — 셋으로 나눈다

**① 데이터에서 파생시킬 수 있는 것**

```
최소관측수 3  →  「순위를 매길 수 있는 최소 개수」는 2다.
                 관측이 1개뿐이면 순위가 성립하지 않는다.
                 그래서 2로 두고, 이것이 방법론적 하한임을 명시한다.
그래프 최소 3점 → 위와 같은 이유로 2점. 선을 그으려면 두 점이면 된다
상위10퍼센트 0.1 → 🔴 **쓰이지 않는 값이다. 지운다**
```

**② 외부 제약에서 오는 것 — 근거를 적는다**

```
우선검토_품목수 5  →  🔴 이것은 판정이 아니라 **화면 제약**이다.
                      「한 장에 담기는 개수」이므로 설정으로 두되,
                      산출물에 「상위 5개만 표시 · 전체 N종 중」을 명시한다.
                      감춘 것이 있음을 사용자가 알아야 한다
도시들[:3]        →  같은 이유. 「외 N곳」을 함께 적어 감춘 수를 밝힌다
```

**③ 남는 것**

없다. 위 두 갈래로 전부 처리된다.

### 🔴 판단이 필요한 지점

`최소관측수` 를 2로 낮추면 **판정 품목이 늘어난다.** 관측 2개로 낸 순위는
신뢰도가 낮은데, 그것을 「임계치 없음」과 맞바꾸는 것이 옳은가.

**대안** — 값은 2로 두되 **관측 수를 산출물에 함께 표시**한다.
「6년 중 1번째」와 「2년 중 1번째」가 화면에서 구분되면 사용자가 스스로 판단한다.

---

## 하지 않을 것

- **품목 확장** — 이 7건을 고친 뒤에 한다. 지금 늘리면 틀린 문장이 6배가 된다
- **디자인 변경** — 서식은 확정됐다. ⑥ 때문에 표시 항목이 하나 늘 뿐이다

---

## 검증 방법

```
1. tests/test_loop.py           기존 16건 유지
2. tests/test_judgment.py (신규)
     · 같은 입력 두 번 → 같은 JSON (④)
     · 증감률 부호와 문장 방향 일치 (⑥)
     · 부분 응답이 ok=False (②)
     · 지역만 바뀐 입력의 지문이 다름 (③)
     · 스냅샷만으로 2회 실행 → 두 번째는 「변화 없음」 (③)
3. 실제 산출물 재생성 후 육안 확인
```

---

## 예상 시간

```
A ④  40분     구조 변경이라 파급이 있다
B ⑥  40분     두 축 분리 + 문장 재작성
B ②  20분
C ①  10분
C ③  20분
D ⑦  15분     문서만
E ⑤  30분     판단 필요
시험  30분
────────────
계   약 3시간 15분
```

마감까지 16시간이므로 여유가 있다. **다만 ⑤의 판단이 늦어지면 A~D 를 먼저 하고
⑤를 마지막에 붙인다.**
