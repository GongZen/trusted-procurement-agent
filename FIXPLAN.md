# 수정 계획 v2 — PR #14 blocker 11건

> 개정 2026-07-28 22:30 · **2차 검토 반영.** 1차 계획은 ⑤가 기각됐고 결함 4건이 추가됐다.
> 근거: [PR #14 2라운드 검토](https://github.com/GongZen/trusted-procurement-agent/pull/14)
> 현재 판정 **12개 조건 중 ✅6 / ❌6**

---

## 1차 계획에서 무엇이 틀렸나

**⑤ 「상위 5개만 표시했다고 밝힌다」** — 기각됐다.
**숨김을 공개하는 것은 임계치를 없애는 것이 아니다.** 자르는 숫자가 그대로 남는다.

이 지적을 받아 **자르지 않는 방식**으로 바꾼다. 아래 E 참조.

---

## 결함 11건과 원인 계통

같은 뿌리에서 나온 것끼리 묶는다. **뿌리를 고쳐야 재발하지 않는다.**

```
뿌리 1 · 결정성이 깨졌다
   ④ 전역 상태(단위기억)
   ⑨ 정렬 동점 순서가 PYTHONHASHSEED 에 따라 달라짐

뿌리 2 · 실패를 성공으로 확정한다
   ② fetch_all 부분 응답이 ok=True
   ⑩ 파이프라인실행이 수집 실패를 삼키고 True 반환
   ③ 지문이 갱신되어 다시 안 받음

뿌리 3 · 자료의 출처·시점이 판정과 어긋난다
   ① 경로 우선순위 (동봉본이 새 수집본을 이김)
   ⑧ 감지한 기준일이 수집기에 전달되지 않음
   ③ 지문에 지역·시장 식별자 누락

뿌리 4 · 말이 숫자를 배신한다
   ⑥ 순위 라벨을 평균 대비로 번역
   ⑥' 「최고가 아님」을 「평년 수준」으로 바꿔 말함
   ⑥'' 「거래처를 바꿔도 소용없다」가 공개 가격으로 뒷받침되지 않음

뿌리 5 · 사람을 기다린다
   ⑦ AGENT.md 가 키 없을 때 request_env_var 호출
   ⑪ 「이월분 재개」가 화면으로 연결되지 않음

뿌리 6 · 우리가 고른 숫자가 대상을 가른다
   ⑤ 최소관측수 3 · 우선검토 5 · 상위10% 0.1 · 도시 [:3] · 그래프 3점
```

---

## A. 뿌리 1 — 결정성 회복

### A-1. ④ 전역 상태 제거

`같은달_관측()` 의 반환에 단위를 함께 담고 **모듈 전역 `단위기억` 을 지운다.**

```python
# 전
버킷[키] = [("2021", 4500.0), ...]        # + 전역 단위기억[키] = "10kg"

# 후
버킷[키] = {"관측": [("2021", 4500.0), ...], "단위": "10kg"}
```

`품목별_집계()` 는 인자로 받은 버킷에서만 읽는다. **함수 밖 상태를 보지 않는다.**

### A-2. ⑨ 정렬 동점 순서 고정

`set` 을 점수 하나로 정렬한 자리가 **네 곳**이다.

```python
# 전 — 점수가 같으면 순서가 해시 시드에 좌우된다
sorted({c["지역"] for c in 목록}, key=lambda v: -max(...))

# 후 — 이름을 보조 키로 둔다
sorted({c["지역"] for c in 목록}, key=lambda v: (-max(...), v))
```

**대상** — `사람말로()` 의 도시·품종 정렬, `품목별_집계()` 의 구간 정렬, 결과 정렬.

### A-3 통과 기준

```
PYTHONHASHSEED=101 / 202 / 303 에서 각각 실행
→ 생성시각을 뺀 report.json 이 바이트 단위로 같아야 한다
```

---

## B. 뿌리 2 — 실패를 실패로 다룬다

### B-1. ② `fetch_all` 부분 응답

```python
if 중단됨 or len(rows) != total:
    return CallResult(op, ok=False, total=total, rows=rows,
                      error=f"부분 응답: totalCount={total} 수신={len(rows)}")
```

**예외** — 첫 페이지만 쓰는 호출(`rows` 를 지정한 `call()`)에는 적용하지 않는다.
`fetch_all` 은 「전량을 받겠다」는 선언이므로 부분은 실패다.

### B-2. ⑩ `파이프라인실행` 이 실패를 삼키지 않게

```python
# 전 — 수집이 실패해도 run.py 만 성공하면 True
if 이름 == "수집": continue

# 후 — 수집 실패를 반환값에 남긴다
됨 = 수집성공 and 판정성공
```

**기존 수집본으로 판정은 시도하되, 반환은 False 다.**
그래야 `agent.py` 가 지문을 갱신하지 않고 다음 주기에 다시 받는다.

### B-3. `collect.py` 가 온전하지 않은 결과를 파일에 쓰지 않는다

```python
if not r.ok:
    기록["실패"].append(...)
    continue          # 기존 파일을 그대로 둔다. 덮어쓰지 않는다
```

### B-4 통과 기준

```
2페이지에서 429 를 강제 → fetch_all 이 ok=False
→ collect.py 종료코드 비정상 → agent.py 지문 미갱신
→ 다음 주기가 같은 자료를 다시 시도
```

---

## C. 뿌리 3 — 출처와 시점을 판정에 맞춘다

### C-1. ① 경로 우선순위 뒤집기

```python
# 전 — 이름(슬림 여부)이 먼저. 다른 위치의 슬림본이 이 위치의 원본을 이긴다
for 후보이름 in (f"{이름}_slim", 이름):
    for base in (SAMPLE, SKILL / "sample-data"):

# 후 — 위치가 먼저
for base in (SAMPLE, SKILL / "sample-data"):
    for 후보이름 in (f"{이름}_slim", 이름):
```

### C-2. ⑧ 감지한 기준일을 수집기에 넘긴다

```python
# agent.py
파이프라인실행(기준일=결과.기준일)
    → subprocess.run([python, "collect.py", 기준일])
```

**`collect.py` 는 벽시계를 쓰지 않는다.** 인자가 없을 때만 오늘을 쓴다.
이것이 「산지 0건」의 원인이었다 — 오늘로 받았는데 자료는 어제까지만 있었다.

### C-3. ③ 지문에 식별 열 전부 포함

```python
f"{exmn_ymd}|{ctgry_cd}|{item_cd}|{vrty_cd}|{grd_cd}|{se_cd}"
f"|{sgg_cd}|{mrkt_cd}|{exmn_dd_prc}"
```

### C-4. ③' 스냅샷에도 지문을 만든다

```python
except ApiKeyMissing:
    지문 = 스냅샷지문()      # sample-data 파일들의 내용 해시
    if 지문 == 상태["지문"]:
        return 변화없음      # 심사자가 두 번 돌려도 두 번째는 건너뛴다
```

**심사자는 키 없이 돌린다.** 지금은 매번 「새 자료 처리」가 나와
**데모에서 보여줄 기능이 정작 심사 환경에서 안 보인다.**

### C-5. 산출물에 출처를 남긴다

```
자료 출처   동봉 스냅샷(2026-07-27)   또는   실시간 수집(2026-07-27)
```

### C-6 통과 기준

```
· TPA_DATA_DIR 에 새 파일을 두면 그것이 선택된다
· 지역만 맞바꾼 두 입력의 지문이 다르다
· 키 없이 2회 실행 → 2회차가 「변화 없음」
```

---

## D. 뿌리 4 — 말이 숫자를 배신하지 않게

### D-1. ⑥ 두 축을 분리한다

**원인** — 순위 라벨을 평균 대비로 번역했다. 둘은 다른 축이다.

```
과거 {100, 200, 300, 400, 500}, 올해 350
   순위    3/6   → 하위권
   평균대비 +17%  → 평년보다 높음
   둘 다 사실이다
```

```python
@dataclass
class 단계판정:
    자리: str   # "6년 중 3번째"          — 순위에서만
    대비: str   # "평년보다 높음" +17%     — 평년평균 대비 부호에서만
```

- `단계해석()` 은 **`대비` 만 읽는다.** 「어디서 벌어졌나」는 평균 대비 이야기다
- `자리` 는 화면에 함께 표시하되 해석에 쓰지 않는다
- **둘 다 임계치가 없다** — 순위는 세는 것, 부호는 데이터가 정한다

### D-2. ⑥' 「최고가 아님」 ≠ 「평년 수준」

```
"애호박 품종은 평년 수준입니다"   ← 실제로는 +15%, 3/6위
```

**평년 대비 부호로만 판정**한다. 최고가 목록에 없다는 사실은 쓰지 않는다.

### D-3. ⑥'' 관측 밖 권고를 내린다

```
전  "거래처를 바꾸는 것으로는 달라지기 어려운 종류입니다"   ← 단정
후  [확인해 보실 것] "산지 단계에서도 높게 관측됩니다.
                      거래처 변경으로 해소되는 종류인지 확인이 필요합니다"
```

**공개 가격만으로는 거래처 변경의 효과를 알 수 없다.** 확인 항목으로 낮춘다.

### D-4 통과 기준

```
모든 품목에서 증감률 부호와 문장 방향이 일치한다 (시험으로 고정)
```

---

## E. 뿌리 6 — 자르는 숫자를 없앤다 🔴 **1차 계획에서 기각된 부분**

### E-1. 우선검토 품목 수 — **자르지 않는다**

```
전  상위 5개를 표시한다                    ← 5 를 우리가 골랐다
후  「최근 N년 최고가인 도시가 1곳 이상」인 품목 전부
```

**「최고가 도시가 있는가」는 예/아니오이고 데이터가 답한다.**
몇 개가 나올지는 그날의 자료가 정한다. **0개면 아무것도 안 나온다** —
그게 「오늘은 볼 것 없다」의 정확한 의미다.

화면이 길어지는 것은 캐러셀이므로 문제가 되지 않는다.

### E-2. 도시 목록 `[:3]` — **자르지 않는다**

최고가인 도시를 **전부 적는다.** 많으면 많은 대로 사실이다.

### E-3. 최소관측수 — **2로 낮추고 관측 수를 표시한다**

```
2 = 순위가 성립하는 최소 개수. 1개로는 순위를 매길 수 없다
```

이것은 우리가 고른 값이 아니라 **방법의 하한**이다.
대신 **관측 수를 화면에 항상 표시**해 「6년 중 1번째」와 「2년 중 1번째」를 구분한다.

### E-4. 상위10퍼센트 `0.1` — **삭제**

계산만 하고 **아무 데도 쓰이지 않는다.** 지운다.

### E-5. 그래프 최소 점수 — **2로**

선을 그으려면 두 점이면 된다.

### E-6 통과 기준

```
코드 전체에서 「대상을 가르는 상수」를 grep 해 0건
설정 파일에 남는 것은 수집 범위(품목 목록·기준연수)뿐이며,
그것이 판정 결과의 순서를 바꾸지 않음을 시험으로 보인다
```

---

## F. 뿌리 5 — 플랫폼 래퍼가 사람을 기다리지 않게

### F-1. ⑦ 키 요청을 예약 루프에서 뺀다

```
전  trigger_prompt 1. DATAGO_KEY 가 없으면 request_env_var 로 요청한다
후  trigger_prompt 1. 키가 있으면 쓰고, 없으면 곧바로 스냅샷으로 진행한다. 묻지 않는다
```

키 요청은 **`SKILL.md` 의 「준비」 절**로 옮긴다 — 사람이 대화로 부를 때만 한다.

### F-2. ⑪ 「이월분 재개」를 화면으로 연결한다

```
변화없음   → 끝낸다
새자료·이월재개 → create_artifact 로 화면을 띄운다
실패       → 상태만 보고한다
```

---

## G. 검토가 짚은 consider 3건

- **`package.py` 필수 파일 검사** — `SKILL.md`·`README.md` 없으면 멈춘다. 제출 형식 누락은 탈락이므로 **막는 편이 안전하다.** 반영한다
- **hallmark 사본 212개 파일** — `.claude/skills/` 가 커밋돼 있다. **제출 zip 에는 안 들어가지만** 저장소가 지저분하다. `.gitignore` 로 뺀다
- **`openpyxl` 설명 정정** — `import_code_tables.py` 하나만 쓴다. **「주 실행 경로는 표준 라이브러리만」** 으로 문구를 고친다

---

## 검증 방법

```
tests/test_loop.py           기존 16건 유지
tests/test_judgment.py (신규)
   A  PYTHONHASHSEED 3종에서 report.json 동일
   A  같은 프로세스에서 순서 바꿔 호출해도 단위 동일
   B  2페이지 429 → fetch_all ok=False → 지문 미갱신
   C  TPA_DATA_DIR 의 새 파일이 동봉본을 이긴다
   C  지역만 맞바꾼 입력의 지문이 다르다
   C  키 없이 2회 → 2회차 변화없음
   D  모든 품목에서 증감률 부호와 문장 방향 일치
   E  대상을 가르는 상수 grep 0건
```

---

## 순서와 예상 시간

```
A 결정성      50분   ④⑨ — 다른 수정의 검증 기반이므로 먼저
B 실패 처리   40분   ②⑩ + collect 저장 규칙
C 출처·시점   50분   ①⑧③③'
D 문장        50분   ⑥⑥'⑥''
E 임계치      40분   자르지 않는 쪽으로 재설계
F 래퍼        20분   문서
G consider    20분
시험          40분
──────────────────
계          약 5시간
```

22:30 시작이면 **03:30 완료.** 마감(14:00)까지 여유가 있다.

---

## 하지 않을 것

- **품목 확장** — 11건을 고친 뒤에 한다
- **일괄 패치 목록** (별도 보관, 이번 범위 아님)
  1. 「가장 싼 도시」를 중도매 구간으로 고정
  2. 산지발 → 원산지 분포 변화(`plor_nm`)
  3. 유통발 → 도매시장 11곳 비교
  4. 데모용 「어제 스냅샷」 동봉
