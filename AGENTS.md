# AGENTS.md — 이 저장소에서 일하는 AI를 위한 지침

> **AI 코딩 도구는 세션을 시작할 때 이 파일을 자동으로 읽는다.**
> 목적은 하나다 — **사람이 "진행 상황을 확인해"라고 설명하지 않아도 맥락을 얻게 하는 것.**
>
> 이 저장소는 사람 1명이 AI 2개(Claude · Codex)를 운영한다. 각 AI는 매번 새 세션으로 시작하므로
> 맥락을 세션에 쌓을 수 없다. 그래서 **맥락을 저장소에 외재화**한다. 아래 절차가 그 장치다.

---

## 0. 세션 시작 절차 — 작업 전에 반드시 실행한다

```bash
gh issue list --state open              # 미완료 작업이 무엇인가
gh pr list --state open                 # 진행 중인 PR과 검토 판정 상태
git log --oneline -10                   # 완료된 과거
gh issue list --state closed --limit 5  # 이미 끝난 일 (다시 제안하지 않기 위해)
```

**열린 PR이 있으면 그 검토 내역까지 읽는다.** 이전 라운드에서 무엇이 지적됐고 무엇이 해소됐는지 모르면 같은 지적을 반복하게 된다.

```bash
gh pr view <번호> --json reviews --jq '.reviews[-1].body'
```

> **git은 과거(완료된 것), GitHub Issue는 현재·미래(미완료·다음 단계)를 담는다.**
> 두 곳을 다 보지 않으면 절반만 아는 상태가 된다.

---

## 1. 읽을 문서

판단하기 전에 관련 문서를 직접 연다. 요약된 기억에 의존하지 않는다.

| 문서 | 담는 것 | 언제 읽나 |
|---|---|---|
| [README.md](README.md) | 문제 정의, **답하지 않는 것**, 스스로 깬 전제 | 설계를 제안하기 전 (이미 기각된 것을 다시 제안하지 않기 위해) |
| [DATA_CRITERIA.md](DATA_CRITERIA.md) | 데이터 선정 조건, **실측 결과와 판정 기록** | 데이터·측정과 관련된 모든 판단 |
| [Scaffolding.md](Scaffolding.md) | 진행 단계와 **잠가야 할 결정 영역** | 결정·순서와 관련된 모든 판단 |
| [DECISIONS.md](DECISIONS.md) | 이미 내려진 결정과 **폐기된 결정의 경계** | 번복하려 할 때 (근거가 필요하다) |
| [DATA_SOURCES.md](DATA_SOURCES.md) | API 엔드포인트·파라미터·재현 절차 | 데이터를 수집할 때 |
| [COLLABORATION.md](COLLABORATION.md) | 협업 절차 + 🔑 **§0 진행 상황** | **새 세션은 여기부터 읽는다** |

> 🔴 **현재 정의:** 식자재 조달 **점검 우선순위**를 정하는 Agent · 데이터는 **공공데이터포털 API 13종**.
> 확정 사항·실측 결과·플랫폼 제약·미확정 결정의 순서는 전부 **[COLLABORATION.md](COLLABORATION.md) §0**에 있다.
> [DECISIONS.md](DECISIONS.md)에는 **폐기된 결정도 남아 있다.** 유효/폐기 표시를 확인하고 읽는다.
> `PROJECT_BRIEF.md`와 `analysis/measure_premises.py`는 삭제됐다 — 링크가 보이면 오래된 참조다.

---

## 2. 작업 규칙

1. **Issue의 완료 조건을 전부 충족시킨다.** 하나라도 못 지켰으면 PR 본문에 미충족으로 정직하게 적는다.
2. **Issue의 범위 밖은 건드리지 않는다.** 개선하고 싶은 것이 보이면 후속 Issue로 제안한다.
3. **🔒 구현 중에 새 결정을 만들지 않는다.** 결정이 필요해지면 그 자리에서 판단하지 말고 [Scaffolding.md](Scaffolding.md) §2의 결정 영역과 [DECISIONS.md](DECISIONS.md)를 확인한다. 거기에 없는 새 결정이 필요하면 **작업을 멈추고 그 사실을 PR 본문에 적는다.**
7. **재지 않은 것을 확정값처럼 쓰지 않는다.** 측정하지 않았으면 **「미측정」**, 원인을 모르면 **「원인 미상」**으로 표기하고 그 상태로 진행한다. 건수가 맞는다는 것만으로 원인을 단정하지 않는다.
4. **`main`에 직접 push하지 않는다.** 모든 변경은 브랜치 → PR을 거친다.
5. **모든 산출물은 한국어로 작성한다.** 코드 식별자와 기술 고유명사는 원문을 쓴다.
6. **설명할 수 없으면 채택하지 않는다.** 기술을 고를 때 *"비전공자가 5분 안에 설명할 수 있는가"*를 판정 기준에 포함한다. 통계적으로 우수해도 **설명이 어려우면 그 자체로 감점 요인**이다.

---

## 3. 검토 규칙

> **검토의 목적은 문서·코드를 완벽하게 만드는 것이 아니라, 다음 단계를 안전하게 시작할 수 있게 만드는 것이다.**

이 규칙은 실제 사고에서 나왔다. 한 PR에서 검토가 4라운드까지 반복됐고, 매 라운드 지적은 타당했지만 *"완전히 무모순인 산출물"*을 목표로 삼는 순간 **종료 조건이 사라졌다.** 파고들면 지적할 것은 항상 나오고, 그동안 프로젝트는 진행되지 않는다.

1. **blocker는 좁게 정의한다** — *"이 상태로 다음 단계를 시작하면 실제로 막히거나, 잘못된 산출물이 나오거나, 되돌리기 어려운 피해가 생기는 것."*
   판정 전에 스스로 묻는다: *이걸 안 고치고 가면 구체적으로 무엇이 잘못되는가?* 답할 수 없으면 blocker가 아니다.
2. **표현 불일치·문언상 충돌은 consider 이하로 내린다.** 이런 유형만으로 PR을 막지 않는다.
3. **재지적하지 않는다.** 이전 라운드에서 해소 판정된 항목을 다시 blocker로 올리지 않는다.
4. **완료 조건 밖의 기준을 만들지 않는다.** 더 나은 방향은 후속 Issue 제안으로 적는다.
5. **라운드 상한은 2회.** 3라운드부터는 남은 지적을 후속 Issue로 넘기고 승인한다.
6. **그럼에도 형식적 승인은 하지 않는다.** 위 규칙은 기준을 낮추는 것이 아니라 **기준의 대상을 좁히는 것**이다.

### 라운드 수와 무관하게 항상 blocker인 것

- 같은 입력에 다른 결과가 나오는 것
- 사람을 기다리며 루프가 멈추는 설계 (**사용자 입력을 요구하며 멈추는 것 포함**)
- 데이터가 뒷받침하지 못하는 수치·용어 사용
  - 재지 않은 것을 확정값으로 쓰기 (예: 결측률을 측정하지 않고 *"완전 연도"*라고 세는 것)
  - 측정하지 않은 선행성으로 시점을 권고하기 (예: 기상 데이터로 *"지금 사라"*고 말하는 것)
  - 내부 데이터가 필요한 판단을 외부 데이터로 하기 (예: 수익성 없이 *"취급을 중단하라"*고 말하는 것)
- 플랫폼 독립성 규칙 위반 ([Scaffolding.md](Scaffolding.md) §1 참조)
- **재현 불가능한 데이터 사용** — 제3자가 자기 인증키로 받을 수 없는 소스를 본체로 쓰는 것
- 🔴 **인증키·자격증명이 저장소나 제출물에 포함되는 것** — 참가 서약의 보안 조항 위반이다. 키는 홈 디렉터리에 두고 `.gitignore`로 차단한다 ([Scaffolding.md](Scaffolding.md) §0.1)
- 공개 범위 규칙 위반 (아래 §4)

---

## 4. 로컬 지침 파일

작업 환경에 아래 파일이 있으면 **함께 읽고 그 지침을 우선 적용한다.** 저장소에는 포함되지 않는다.

- `CLAUDE.md` — Claude용 상세 지침
- `CODEX.md` — Codex용 상세 지침
- `_local/Skillthon.md` — 대회 요건 원문 정리 (배점·심사 방식·제출물)

**여기에 공개 범위 규칙이 있다.** 공개 저장소에 올라가는 파일을 작성하거나 검토할 때는 그 규칙을 먼저 확인한다. 위반은 무조건 blocker다.

로컬 지침이 없는 환경이라면 이 문서의 규칙만으로 작업하되, **공개 범위 판단이 필요한 내용은 임의로 쓰지 말고 사람에게 확인한다.**

---

## 5. 소통 방식

- 답변은 **3~10줄**. 결론 먼저, 근거는 한 줄.
- 작업 응답 맨 위에 `지금 여기: 단계 · 하는 것 · 왜` 한 줄.
- **선택지를 나열하지 않는다.** 열린 질문으로 묻고, 추천이 있으면 **하나만** 말한다.
- 긴 내용은 문서·PR에 쓰고 대화에는 요약 3줄 + 링크.
- **한 응답 = 하나의 핵심 질문·결정.** 단 사람이 여러 항목을 요청하면 모두 간단히 답한다.
