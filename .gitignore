# ─── 제3자 원본 데이터셋 ───────────────────────────────
# 용량이 크고 재현 스크립트로 다시 받는다 (DATA_SOURCES.md 참조)
adventureworks/
contoso/
data/

# ─── 로컬 전용 ────────────────────────────────────────
# 개인 공간·참고 자료·보관 문서는 이 폴더 하나로 모은다
_local/

# AI 행동 지침 — 루트에 있어야 각 도구가 자동으로 읽으므로 _local/로 옮기지 않는다
CLAUDE.md
CODEX.md

# 공개 전 재작성이 필요한 도식
assets/

# ─── AI 실행 로그 ─────────────────────────────────────
# 검토 과정의 원본 기록. 판정 결과는 PR에 남으므로 로그는 로컬에만 둔다
logs/

# ─── 실행 환경 ────────────────────────────────────────
__pycache__/
*.py[cod]
.venv/
venv/
.env
.ipynb_checkpoints/
.DS_Store
